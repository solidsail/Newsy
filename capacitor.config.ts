import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.solidsail.newsy",
  appName: "newsy",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
